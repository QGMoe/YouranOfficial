# 2026年6月25日

## 更改

### Sponge

#### 生成

1. 怪物和动物生成周期从 600 刻下调为 400 刻；
2. 水生生物相关生成周期从 50 下调到 10 刻；
3. 怪物生成周期从 20 下调到 2 刻。

#### 方块实体

1. 默认方块实体加载范围从 40 方块向上调整为 160 方块；
2. 炼金化学、Bithop 、家具模组、区块加载器模组、快速熔炉模组、工业时代2、RF 工具箱、沉浸工程、SCP模组、静谧四季、匠魂所有加载范围低于 160 的方块实体加载范围均向上调整到 160 方块；
3. 除 immersivepetroleum:autolubricator 保留 0 方块外，沉浸石油其他方块实体加载范围均上调到 160 方块；
4. 工业线缆方块实体加载范围均上调到 256 方块；
5. 原版的光照检测器、附魔台、末地传送门方块实体加载范围上调到160方块，默认值也调整为 160 方块，头颅加载范围上调到96方块；
6. 夸克模组的 smoker 方块实体上调到 96 方块加载范围；
7. 暮色森林的 smoker 和 trophy 方块实体加载范围上调到 160 方块，其余包括默认值上调到 96 方块。

#### 自动保存

1. 玩家数据自动保存间隔由 24000 刻下调到 2400 刻；
2. 区块数据自动保存间隔由 36000 刻下调到 9000 刻。

#### 世界

1. 主世界视距上调到 16 区块；
2. 下界视距上调到 12 区块；
3. 失落的城市视距上调到 12 区块；
4. 暮色森林视距上调到 12 区块；
5. 主城维度视距上调到 32 区块。

### 原版服务器配置

1. 默认视距上调到 14 区块。

## 移除

1. 移除了泡沫修复以提升兼容性；
2. 移除了 AI 改进以提升兼容性。